# 2026-07-17 Daily Memory updates
- Fixed layout issue: replaced horizontal PageView with a vertical ListView.builder in `live_screen.dart` when grid size is 1x1.
- Resolved Yoosee camera playback: mapped Camera 3's codec metadata on list call to `yoosee_h264`, forcing the Flutter client to use the stream endpoint `/api/playback/stream`. Server-side stream remuxes-copy video and drops problematic audio (`-an`) and fixes timestamps using fragmented MP4, saving it to a clean cache path on disk `/mnt/camera_storage/recordings/cache/3/...`.
- Fixed 9router AI test connection: increased `urlopen` timeout from 15 to 45 seconds on backend (`app.py`), and increased connection timeout on Flutter `api_service.dart` from 25 to 60 seconds to handle slow reasoning routes (which take around 18s).
- Added Telegram config screen: introduced the Telegram Tab inside `ConfigScreen` with fields `telegram_bot_token`, `telegram_chat_id`, test message trigger, and state preservation.
- Fixed face event image display: updated `authHeaders` in `api_service.dart` to send tunnel-bypass headers (`Bypass-Tunnel-Reminder`, `ngrok-skip-browser-warning`, and browser `User-Agent`) to prevent tunnel warning page blocks on static face image loads.
- Redesigned Playback view for Event-based scrubbing: added an event-based list option in `playback_screen.dart` displaying AI face/motion events. Clicking an event parses its timestamp, identifies the containing 10-minute video segment, seeks to the exact event offset, and plays instantly. Added a toggle button to switch back to full 24/24 files.
- Pushed changes to GitHub repository `git@github.com:dthanhsang/nvrios.git` on branches `main` and `dvr_flutter_ios_xcode`.
